<launch>
  <!-- By default we do not overwrite the URDF. Change the following to true to change the default behavior -->
  <arg name="load_robot_description" default="false"/>

  <!-- Load universal robotic description format (URDF) -->
  <include if="$(arg load_robot_description)" file="$(find kurt_description)/launch/upload_kurtana.launch" />

  <!-- The semantic description that corresponds to the URDF -->
  <param name="robot_description_semantic" textfile="$(find kurtana_moveit_config)/config/kurtana.srdf" />
  
  <!-- Load to the parameter server yaml files -->
  <group ns="robot_description_planning">    
    <rosparam command="load" file="$(find kurtana_moveit_config)/config/joint_limits.yaml"/>
  </group>
  
</launch>
